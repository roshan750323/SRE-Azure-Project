pipeline {
  agent any
   stages{
    stage('deploy') {
      agent {
        docker {
          image 'ubuntu:latest'
        }
      }
      steps {
        sh 'echo "Hello"'
        sh 'sudo apt-get update'
      }
    }
  }
}
