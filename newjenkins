pipeline {
   agent {
    docker {
    	image 'ubuntu:latest'
        }
   }
   stages{
    stage('deploy') {
        }
      steps {
        sh 'echo "Hello"'
        sh 'sudo apt-get update'
      }
    }
  }
